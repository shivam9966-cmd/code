forecasting_table_info = {
    "schema": "finance",
    "table_name": "forecast_data_new",
    "table_description": (
        "Capital Markets revenue forecasting fact table in long format. "
        "Each row is a monthly revenue amount for a specific Version/Client/Product combination. "
        "Data covers Capital Markets business unit only. "
        "Version indicates the planning scenario: '1+11' or '2+10'. "
        "Time is stored as separate Year (INTEGER) and month (VARCHAR) columns."
    ),
    "columns": {
        "version":                "VARCHAR — planning scenario, values: '1+11' or '2+10' only",
        "Year":                   "INTEGER — calendar year e.g. 2026",
        "month":                  "VARCHAR — e.g. 'Jan', 'Feb', ..., 'Dec'",
        "amount":                 "NUMERIC — revenue amount, use SUM(COALESCE(\"amount\", 0))",
        "entity_name":            "VARCHAR — client/customer name",
        "ems_id":                 "VARCHAR",
        "opportunity_id":         "VARCHAR",
        "product_combo":          "VARCHAR — product ID",
        "product_description":    "VARCHAR — most granular product description",
        "l4_description":         "VARCHAR — product hierarchy level 4",
        "l5_description":         "VARCHAR — product hierarchy level 5",
        "l6_description":         "VARCHAR — product hierarchy level 6",
        "l7_description":         "VARCHAR — product hierarchy level 7",
        "l8_description":         "VARCHAR — product hierarchy level 8",
        "l7_account":             "VARCHAR — top account level: 'Revenue' or 'Operating Expenses'",
        "l8_account":             "VARCHAR — account level 8: 'Recurring Revenue' or 'Non-Recurring Revenue'",
        "l9_account":             "VARCHAR — account level 9 e.g. 'Transaction Processing and Broker Dealer Revenue'",
        "l10_account":            "VARCHAR — most granular account level",
        "ruc_risk":               "VARCHAR — values: 'RUC', 'Risk ID', 'Risk UID'",
        "attrition_description":  "VARCHAR — values: 'Known attrition', 'Specific attrition', 'attrition provision'",
        "icp_hfm":                "VARCHAR — attrition code",
        "location":               "VARCHAR — location code",
        "ora_account":            "VARCHAR — account ID",
        "ora_company":            "VARCHAR — company ID",
        "cost_center":            "VARCHAR — cost center code",
    },
    "query_rules": [
        "Query finance.forecast_data_new. Always use finance. schema prefix.",
        "Do not use columns that do not exist: month_code, month_date, LOCATION, ENTITY, Version, entity, amount_usd.",
        "Use amount for all revenue aggregations: SUM(COALESCE(\"amount\", 0)).",
        "Time filtering: use \"Year\" (INTEGER) and \"month\" (VARCHAR e.g. 'Jan'). Do not use DATE_TRUNC or date functions.",
        "Version column holds planning scenario: '1+11' or '2+10' only. Exclude 'Actuals' and 'Plan'.",
        "Latest version = higher number before '+'. '2+10' is later than '1+11'.",
        "Dynamic latest version subquery: (SELECT \"version\" FROM finance.forecast_data_new WHERE \"version\" IN ('1+11','2+10') ORDER BY CAST(SPLIT_PART(\"version\", '+', 1) AS INTEGER) DESC LIMIT 1)",
        "REVENUE FILTER: include \"l7_account\" = 'Revenue' in all revenue queries unless user asks for OpEx or EBITDA.",
        "RECURRING REVENUE: filter \"l8_account\" = 'Recurring Revenue'. NON-RECURRING: filter \"l8_account\" = 'Non-Recurring Revenue'.",
        "SPECIFIC ACCOUNT: filter \"l9_account\" for specific account element queries.",
        "RUC FILTER: \"ruc_risk\" = 'RUC' AND (\"attrition_description\" IS NULL OR TRIM(\"attrition_description\") = '')",
        "RISK ID FILTER: \"ruc_risk\" = 'Risk ID' AND (\"attrition_description\" IS NULL OR TRIM(\"attrition_description\") = '')",
        "RISK UID FILTER: \"ruc_risk\" = 'Risk UID' AND (\"attrition_description\" IS NULL OR TRIM(\"attrition_description\") = '')",
        "ATTRITION FILTER: \"attrition_description\" IS NOT NULL AND TRIM(\"attrition_description\") != ''",
        "KNOWN ATTRITION: \"attrition_description\" = 'Known attrition'",
        "SPECIFIC ATTRITION: \"attrition_description\" = 'Specific attrition'",
        "ATTRITION PROVISION: \"attrition_description\" = 'attrition provision'",
        "Product drill-down: l4_description -> l5_description -> l6_description -> l7_description -> l8_description -> entity_name.",
        "Account drill-down: l7_account -> l8_account -> l9_account -> l10_account.",
        "GENERAL VARIANCE question: group Q1 by \"l9_account\". Group Q2 (description sql) by \"ruc_risk\",\"l9_account\",\"l5_description\",\"entity_name\",\"location\".",
        "For month output: GROUP BY \"Year\", \"month\" ORDER BY \"Year\" ASC, CASE WHEN month order.",
        "For quarterly output: derive quarter using CASE WHEN on month. Do not select month in quarterly queries.",
        "Quarter mapping: Q1 -> 'Jan','Feb','Mar' | Q2 -> 'Apr','May','Jun' | Q3 -> 'Jul','Aug','Sep' | Q4 -> 'Oct','Nov','Dec'.",
        "TOTALS ROW: every tabular query must have UNION ALL with 'Total'::text label using sort_key=1.",
        "LIMIT only at the very end. Not inside individual SELECT in UNION ALL.",
        "ORDER BY only once at the very end.",
        "VERSION COMPARISON: use CASE WHEN — not IN() for both versions in one SUM.",
        "Variance = 2+10 amount - 1+11 amount. Variance % = variance / NULLIF(1+11 amount, 0) * 100.",
        "ROUNDING: ROUND((SUM(COALESCE(\"amount\",0))/1000000.0)::numeric, 2) — ::numeric cast AFTER dividing by 1000000.0.",
        "Double-quote ALL column names in SQL.",
        "No dangerous keywords: drop, delete, truncate, alter, update, insert, create, replace, grant, revoke.",
    ],
}
