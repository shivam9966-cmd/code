exclude_prefixes = ("account", "product", "cc")


new_cols = []

for c in df_csv.columns:
    if c.lower().startswith(exclude_prefixes):
        new_cols.append(F.col(c).alias(c))        # keep as-is
    else:
        new_cols.append(F.col(c).alias(c.lower()))  # lowercase
