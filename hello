from pyspark.sql import functions as F

# Remove 2022 and 2023
filtered_df = granular.filter(~F.year(F.col("DATE")).isin(2022, 2023))

new_total_count = filtered_df.count()

print("Total rows after removing 2022 & 2023 data:", new_total_count)

