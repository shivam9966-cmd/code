

OUTPUT_PATH = "Files/SQL_DUMPS_SMALL_ACCOUNTS"   # <-- choose any folder name you like

# Save as CSV with header
(subset.coalesce(1)  # merges all partitions into 1 file for easy reading
       .write
       .mode("overwrite")
       .option("header", "true")
       .csv(OUTPUT_PATH))

print(f"✅ File saved successfully at: {OUTPUT_PATH}")
