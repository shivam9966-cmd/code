# Load the granularbug dataset (adjust the path if it’s stored differently)
granularbug = spark.read.csv("Files/SQL_DUMPS/granularbug", header=True, inferSchema=True)

# Show 20 rows in tabular form
display(granularbug.limit(20))
