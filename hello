import os
os.getcwd()


os.listdir()




import os

for root, dirs, files in os.walk("/", topdown=True):
    for name in files:
        if "smallcase" in name.lower():
            print(os.path.join(root, name))

