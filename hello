output_path = "Files/FINAL_SINGLE_CSV"

(df_clean
     .coalesce(1)                 # <- forces ONE CSV file
     .write
     .mode("overwrite")
     .option("header", True)
     .csv(output_path)
)

print("Single CSV saved to:", output_path)
