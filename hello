from pyspark.sql import functions as F

total_2426 = (
    granular
    .withColumn("YEAR", F.year("DATE"))
    .filter(F.col("YEAR").between(2024, 2026))
    .count()
)

print("Total rows from 2024 to 2026:", total_2426)

