from pyspark.sql import Row

counts_df = spark.createDataFrame([
    Row(Table="ACTUALS", Count=actual_count),
    Row(Table="PLAN", Count=plan_count),
    Row(Table="FORECAST", Count=forecast_count)
])

display(counts_df)
