(
    df_clean
        .coalesce(1)                            
        .write
        .mode("overwrite")
        .option("header", True)
        .option("compression", "gzip")
        .csv("Files/FINAL_SINGLE_CSV_GZIP")
)

print("Compressed CSV written.")
